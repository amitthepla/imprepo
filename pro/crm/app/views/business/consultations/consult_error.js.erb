$('.modal').modal('hide');
$('.modal-backdrop').remove();
$('body').removeClass('modal-open');

$.gritter.add({
  title: '<i class="fa fa-exclamation-triangle"></i> Error Booking Consult',
  text: "Unable to book <%=@lead.name%>'s consultation. <%= @consult_errors%>",
  sticky: false,
  time: '5000',
  class_name: 'gritter-danger'
});
