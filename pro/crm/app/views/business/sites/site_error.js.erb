$('.modal').modal('hide');
$('.modal-backdrop').remove();
$('body').removeClass('modal-open');

$.gritter.add({
  title: '<i class="fa fa-exclamation-triangle"></i> Error Saving Site Form',
  text: "Unable to save Site Form. <%= @site_errors%>",
  sticky: false,
  time: '5000',
  class_name: 'gritter-danger'
});
